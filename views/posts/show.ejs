<!DOCTYPE html>
<html>
<head>
    <% include ../partials/head %>
</head>
<body>
<% include ../partials/nav %>

<div class="container-fluid">
    <div class="container post post-show">

        <div class="container" id="review_title">
            <span>후기 읽기</span>
        </div>

        <div class="contentBox">
            <h3 class="contentBoxTop"><%= post.title %></h3>
            <div class="row">
                <div class="col-sm-4 col-sm-push-8">
                    <div class="post-info">
                        <div><span>글쓴이</span> : <%= post.author ? post.author.userid : "" %></div>
                        <div><span>작성날짜</span> : <%= post.createdDate %> <%= post.createdTime %></div>
                        <% if(post.updatedAt) { %>
                        <div><span>수정날짜</span> : <%= post.updatedDate %> <%= post.updatedTime %></div>
                        <% } %>
                    </div> <!-- post-info end -->
                </div> <!-- col end-->
                <div class="col-sm-8 col-sm-pull-4">
                    <div class="post-body"><%= post.body %></div>
                </div> <!-- col end-->
            </div> <!-- row end -->
        </div> <!-- post-container end -->
        <div class="buttons">
            <div class="row">
                <a class="btn btn-warning" href="/posts">뒤로가기</a>
                <% if(isAuthenticated && post.author && currentUser.id == post.author.id){ %>
                <a class="btn btn-info" href="/posts/<%= post._id %>/edit">수정</a>
                <form id="btn-delete" action="/posts/<%= post._id %>?_method=delete" method="post">
                    <a class="btn btn-danger" href="#"
                       onclick="confirm('정말 삭제하시겠습니까?')?this.parentElement.submit():null;">삭제</a>
                </form>
                <% } %>
            </div>
        </div>
    </div> <!-- container end -->
    <% include ../partials/footer %>
</div>

</body>
</html>
